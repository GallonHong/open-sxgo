# WFD 安全约束

本文件适用于公开目录、可信投稿、管理后台、私密 API、治理服务、数据构建、验证器、签署工具和镜像。当前为封闭演示；实现状态和未完成门槛见 docs/STATUS.md 与 docs/governance/AC2.md。本文不代表已完成独立安全审计。

## 信任边界

投稿正文、URL、外部网页、文件、节点响应、第三方代码与协作事件均为不可信输入。公开镜像只分发 PUB 数据，不持有私密数据库、回执、管理员会话、私钥或证据。浏览器内显示的验证图标不能证明该网页自身可信。

数据库中的成员关系属于私密运营数据。账号、项目公钥、真实独立人员、角色授权分别建模；多个账号或密钥不能增加独立批准人数。人员独立性仍需实际核实，数据库不能证明自然人唯一。

## 必须保持的性质

- 高权限请求在服务端校验当前会话、WebAuthn 认证、授权范围、对象、案件分配、利益回避、有效期和撤销状态。不可用时拒绝，不信前端传入的 actor 或资格。
- 所有事实候选同版本独立双审；实质修改使旧决定失效。隐私、主体错误、伪造或钓鱼阻断不能被多数覆盖。
- 贡献数量不直接授权；认定、资格、执行分离。决议通过不等于已经执行；执行时重新验证当前政策和签名依据。
- 来源处理区不得与私密 API、数据库和签名服务共享主机、私网或生产凭据；DNS 全地址、固定连接地址、每跳及网络出口均需校验。无合格隔离时关闭对应抓取，不要求审核员本机访问。
- 私密附件与动态渲染默认关闭，P1 门槛未验收不得开启。净化资料只以受控文本或经批准处理的位图显示，不加载源站资源。
- 公共输出使用严格白名单构造，不包含内部人员关系、回执、原始私密引用、附件哈希或排班。小群体贡献统计及发布时间受隐私政策约束。
- Root、Targets、审核业务签名和登录凭据用途分离。Root/Targets 保持 2-of-3；生产私钥不得进入网页、普通 CI 或抓取区。WebAuthn 不冒充个人 Ed25519 签名。
- 可信元数据单调更新，分叉和回滚拒绝；更新失败保持旧的完整公共副本。历史展示明确时效，不能假称已收到离线期间的撤销。
- 撤权同时阻断后续会话、案件访问及排队任务；紧急操作只撤减权限或暴露，不自动恢复被隐藏的风险内容。

## 审查与报告

可报告事项包括跨对象越权、隐私导出、认证绕过、重复人员凑阈值、签名用途混淆、回滚/分叉接受、SSRF、隔离逃逸、不可控资源消耗、任意代码执行及生产凭据进入不可信执行环境。根据实际可达路径、资产和影响评估严重性；单个测试通过不构成该边界无漏洞的证明。

目前没有以“演示”“已知缺口”或依赖包为理由自动忽略问题的排除项。生产人员、真实 D1/R2、独立副本、网络隔离、备份删除和恢复须另行实测，未验证的控制不得作为抑制问题的依据。

未修复漏洞或私人材料不得提交公开议题。运营方的私密报告入口尚待配置；当前演示不承诺全天值守，也不要求报告者先公开敏感细节。
