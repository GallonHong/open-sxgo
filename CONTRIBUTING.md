# 参与贡献

欢迎提供可核对的公开资料、纠错、代码、文档、无障碍改进、部署恢复演练和节点服务。贡献按实际工作单元认定，不按提交量、代码行数或资源价格换取权限。

请先说明任务目的、已有来源、预期结果及适用范围；不要附带身份证、内部合同、工资条、私人聊天、回执或密钥。漏洞详情仅通过经运营方确认的私密渠道提交；在渠道尚未配置时不要将敏感详情发到公开议题。

本地验证：`pnpm install --frozen-lockfile` 后运行 `pnpm check`。涉及浏览器流程时另运行 `pnpm test:e2e`。测试数据必须明确虚构，测试密钥不得用于生产。

同一目标拆分的机械提交归并认定。共同作者不得互相完成本应独立的资格确认；普通修改由非作者审查，权限、私密数据、协议、签名、发布及 CI 关键路径须两名合格非作者审查者。

不可信代码仅在无生产凭据及私网权限的测试环境运行。合并和生产部署分离；签名根私钥不得进入仓库、普通 CI、抓取区或网页。

成员署名及代码账号关联为自愿选择；匿名投稿申领需要本人回执和明确关联同意，不自动公开具体案件。认定被拒时提供理由及申诉，不以公开真实身份作为贡献前提。
